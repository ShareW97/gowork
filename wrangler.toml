name = "gowork0"
compatibility_date = "2026-06-18"
pages_build_output_dir = "dist"

# Configure these bindings in Cloudflare Pages for both Production and Preview:
# - KV namespace binding: GO_WORK_DATA
# - R2 bucket binding: GO_WORK_IMAGES
#
# Git-connected Cloudflare Pages should use:
# - Build command: npm run build
# - Build output directory: dist
# - Deploy command: leave empty
#
# Do not run a Wrangler deploy command inside a Git-connected Pages build.
# If a manual deploy is needed from a local terminal after `wrangler login`,
# use:
#   npx wrangler pages deploy dist --project-name gowork0
